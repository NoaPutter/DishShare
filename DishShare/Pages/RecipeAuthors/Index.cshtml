@page
@model DishShare.Pages.RecipeAuthors.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RecipeAuthor[0].UserRole)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RecipeAuthor[0].ContributionType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RecipeAuthor[0].CreationDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.RecipeAuthor) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserRole)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ContributionType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreationDate)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.RecipeAuthorID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.RecipeAuthorID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.RecipeAuthorID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
